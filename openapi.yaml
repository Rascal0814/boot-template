# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: EchoService API
    version: 0.0.1
paths:
    /api/echo/say:
        get:
            tags:
                - EchoService
            operationId: EchoService_Say
            parameters:
                - name: text
                  in: query
                  schema:
                    type: string
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/v1.SayResponse'
components:
    schemas:
        v1.SayResponse:
            type: object
            properties:
                text:
                    type: string
tags:
    - name: EchoService
